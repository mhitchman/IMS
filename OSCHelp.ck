// This is here to make it easy to change the port globally across the whole system 
// The port only needs to be set in one place and then changed in that one place

public class OscHelper{
	static int oscPort;
}